apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-test-repo
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: devops-test.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devops-test-repo
                port:
                  number: 80

