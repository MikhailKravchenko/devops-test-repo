apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: devops-test-db
spec:
  selector:
    app: devops-test-db
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  type: ClusterIP

